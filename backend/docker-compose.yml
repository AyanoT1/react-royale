services:
  mongodb:
    image: mongo:latest
    container_name: mongodb
    ports:
      - '27017:27017'
    env_file:
      - .env.local
    volumes:
      - mongo-data:/data/test

volumes:
  mongo-data:
